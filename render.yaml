services:
  - type: web
    name: tenbagger-system
    env: docker
    plan: free

  - type: cron
    name: tenbagger-auto
    env: docker
    plan: starter
    schedule: "0 23 * * *"
    dockerCommand: >
      python -c "import requests; print(requests.get('https://tenbagger-system.onrender.com/run-buy-check?key=buycheck_2026_yutaka_9x7pL2', timeout=60).text)"
